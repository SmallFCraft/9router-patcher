# 9router patches — nguồn duy nhất. Chuỗi find/replace HIỆN TẠI đo trên build 0.5.75.
# 0.5.69 -> 0.5.75: module 5016 (w.N) thêm 3 param (`reqLogger:C`, `toolNameMap:D`, `provider/model`
# đẩy vào đầu) nên minifier dịch tiếp chuỗi định danh 1 chữ: trackDone E->F, appendLog F->G,
# reqTag H->I, log I->J, cục bộ J->K. p11 remap cả find lẫn replace (CẠM BẪY 0.5.65 lặp lại nếu
# chỉ remap find). p19: hàm kế `function s(a)` -> `function t(a)`, thân patch không đổi.
# 0.5.65 -> 0.5.69: closure `function n(a={})` trong 8895.js nhận thêm 2 param (`credentials:y`,
# `sessionId`) nên minifier dịch CẢ chuỗi định danh 1 chữ lên một nấc: B(ctx)->C, L->M, M->N,
# N(cờ)->O, O(hàm flush usage)->P. p6/p7/p8 phải remap; p9 giữ nguyên vì anchor của nó chỉ dùng
# param của hàm (`t,p,u`) — chính vì thế p9 vẫn `clean` khi 3 patch còn lại `dead-anchor`.
#   CẠM BẪY đã sập một lần (sáng 2026-09-06): remap `find` mà giữ `replace` của 0.5.65.
#   `replace` p8 khi đó khai `O=()=>{...}` (O phải là CỜ) và không bao giờ khai `P`, nên 4 lời gọi
#   `P()` còn lại — kể cả 2 lời trong flush() — thành ReferenceError: `node --check` pass, SSE không
#   đóng, gauge activeRequests kẹt. Remap `find` và `replace` CÙNG NHAU, không bao giờ riêng lẻ.
# `order` tường minh: áp theo 1->9.
# `group`: các patch cùng group nằm cùng một file, áp/gỡ atomic (cả nhóm hoặc không cái nào).
# `defines`: patch này định nghĩa symbol mà patch khác trong group gọi -> áp ĐẦU TIÊN, gỡ CUỐI CÙNG.

[[patch]]
id = "connect-timeout-180s"
order = 1
summary = "Connect timeout 60s → 180s"
why = "Node chậm cần lâu hơn mới failover; env var FETCH_CONNECT_TIMEOUT_MS chỉ là lớp thứ hai"
find = 'FETCH_CONNECT_TIMEOUT_MS",6e4'
replace = 'FETCH_CONNECT_TIMEOUT_MS",18e4'

[[patch]]
id = "ua-messages"
order = 2
summary = "Gửi User-Agent claude-cli cho /v1/messages"
why = "AgentRouter trả 401 unauthorized client detected khi UA là \"node\""
find = 'delete h["anthropic-dangerous-direct-browser-access"]'
replace = 'h["User-Agent"]="claude-cli/2.1.92 (external, cli)",delete h["anthropic-dangerous-direct-browser-access"]'

[[patch]]
id = "ua-models-route"
order = 3
summary = "Cùng UA cho provider models fetch"
why = "agentrouter.org/v1/models từ chối UA mặc định (nút refresh models trên trang provider)"
find = 'method:"GET",headers:{"Content-Type":"application/json","x-api-key"'
replace = 'method:"GET",headers:{"Content-Type":"application/json","User-Agent":"claude-cli/2.1.92 (external, cli)","x-api-key"'

[[patch]]
id = "ua-models-agg"
order = 4
summary = "Cùng UA cho per-provider fetch trong /api/models"
why = "chunk dựng header qua d[...] nên cần anchor riêng"
find = 'd["anthropic-version"]="2023-06-01",d.Authorization='
replace = 'd["anthropic-version"]="2023-06-01",d["User-Agent"]="claude-cli/2.1.92 (external, cli)",d.Authorization='

[[patch]]
id = "tools-strip-custom"
order = 5
summary = "Bỏ type:\"custom\" khỏi tools trước khi gửi upstream"
why = "AgentRouter 2026-09-04 trả 400 unknown variant `custom`; dạng no-type được chấp nhận ở mọi nơi"
find = 'function h({provider:a,model:b,body:c}){let i=d.xq[a]?.reasoningInject,j=e.find(a=>a.match(b));'
replace = 'function h({provider:a,model:b,body:c}){if(c&&Array.isArray(c.tools))c.tools=c.tools.map(t=>{if(t&&"custom"===t.type){let u={...t};delete u.type;return u}return t});let i=d.xq[a]?.reasoningInject,j=e.find(a=>a.match(b));'

[[patch]]
id = "sse-close-translate"
order = 6
group = "sse-hang"
summary = "Đóng SSE sau terminal event (nhánh dịch)"
why = "motomoto.lol giữ TCP mở sau message_stop nên Claude Code quay mãi; terminate() bỏ qua flush() nên phải tự phát tail event, ghi usage (P) và giảm gauge ($G)"
find = 'N=!0,s&&(M=!0);continue}'
replace = 'N=!0,s&&(M=!0);{let $z=(0,d.Y8)(c,o,null,C);if($z?.length>0)for(let $y of $z)null!=$y&&f.enqueue(k.encode((0,h.v8)($y,o)));}P(),$G();setTimeout(()=>{try{f.terminate()}catch{}},50);continue}'

[[patch]]
id = "sse-close-passthrough"
order = 7
group = "sse-hang"
summary = "Đóng SSE sau [DONE] (nhánh passthrough)"
why = "client định dạng openai trên /v1/chat/completions dùng sentinel [DONE]"
find = 'q?.appendConvertedChunk?.(b),f.enqueue(k.encode(b)),d&&P();continue}'
replace = 'q?.appendConvertedChunk?.(b),f.enqueue(k.encode(b)),d&&P();if(b&&b.indexOf("[DONE]")>=0){N=!0,P(),$G(),setTimeout(()=>{try{f.terminate()}catch{}},50)}continue}'

[[patch]]
id = "gauge-guard"
order = 8
group = "sse-hang"
defines = "$G"
summary = "Giảm gauge in-flight đúng một lần ($G)"
why = "stream kết thúc qua patch 6/7 vẫn phải xoá dòng calling trên trang Usage"
find = 'O=!1,P=()=>{if(O)return;O=!0;'
replace = 'O=!1,$g=!1,$G=()=>{if($g)return;$g=!0;try{(0,f.uw)(t,p,u,!1)}catch{}},P=()=>{if(O)return;O=!0;'

[[patch]]
id = "gauge-flush-route"
order = 9
group = "sse-hang"
summary = "Đưa decrement lúc flush() qua cùng guard"
why = "tránh giảm gauge hai lần khi cả flush() và patch 6/7 cùng chạy"
find = '(0,f.uw)(t,p,u,!1);try{'
replace = '$G();try{'

# 0.5.69: request non-streaming mà upstream trả text/event-stream rác (AgentRouter thỉnh thoảng
# vậy — chớp ngẫu, gặp ở glm-5.3-flash 17:12:19) → module 5016 convert bằng k.F(81626) ra null →
# 502 "Invalid SSE response for non-streaming request" + kho model/key 30s oan. Group này thêm
# MỘT lần retry: caller (module 92500) nhả closure `retry` (aY.execute stream:true, AbortSignal
# 30s); module 5016 khi k.F=null thì gọi retry, đọc stream TĂNG DẦN (thoát sớm ở message_stop —
# giữ ngữ nghĩa sse-hang), tự aggregate frame anthropic SSE thành message JSON đúng shape
# `a.json()` vốn có rồi đẩy vào J. Mọi nhánh lỗi (retry fail, frame rác, thiếu message_stop,
# timeout) rơi về đúng 502 cũ — từng patch áp một mình cũng chỉ no-op. KHÔNG remap 1-chữ: mọi
# tên inject đều có tiền tố $.

[[patch]]
id = "nonstream-retry-exec"
order = 10
group = "nonstream-sse-retry"
summary = "Nhả closure retry (execute stream:true) vào handler non-streaming"
why = "chỉ caller (92500) có aY.execute; 5016 không tự fetch lại được"
find = 'let a1={provider:ao,model:ap,body:a,stream:aF,translatedBody:ah,finalBody:an,requestStartTime:aq,connectionId:P,apiKey:R,clientRawRequest:O,onRequestSuccess:M,pxpipe:aX,reqTag:as,log:d},a2='
replace = 'let a1={provider:ao,model:ap,body:a,stream:aF,translatedBody:ah,finalBody:an,requestStartTime:aq,connectionId:P,apiKey:R,clientRawRequest:O,onRequestSuccess:M,pxpipe:aX,reqTag:as,log:d,retry:async()=>{let $s2=a$.signal;try{typeof AbortSignal.any=="function"&&($s2=AbortSignal.any([a$.signal,AbortSignal.timeout(30000)]))}catch{}try{let $e=await aY.execute({model:ap,body:typeof ah=="object"&&ah?{...ah,stream:!0}:ah,stream:!0,credentials:c,providerSessionId:ar,clientTool:aM,signal:$s2,log:d,proxyOptions:a_});return $e&&$e.response||null}catch{return null}}},a2='

[[patch]]
id = "nonstream-retry-aggregate"
order = 11
group = "nonstream-sse-retry"
summary = "k.F=null → retry stream:true + tự aggregate anthropic SSE thành JSON thay vì 502"
why = "upstream trả SSE rác cho request stream:false; aggregate reader tăng dần, thoát sớm ở message_stop, mọi lỗi về 502 cũ"
find = 'log:J}){let K;if(F(),(a.headers.get("content-type")||"").includes("text/event-stream")){let b=await a.text(),d=(0,k.F)(b,c);if(!d)return G({status:'
replace = 'log:J,retry:$x}){let K;if(F(),(a.headers.get("content-type")||"").includes("text/event-stream")){let b=await a.text(),d=(0,k.F)(b,c);if(!d&&typeof $x=="function"){let $r=null;try{$r=await $x()}catch{}if($r&&$r.ok){let $m=null;try{if(($r.headers.get("content-type")||"").includes("text/event-stream"))$m=await (async($rp,$mo)=>{let $agg=$t=>{try{let $f=[];for(let $ln of String($t||"").split("\n")){let $s3=$ln.trim();if(!$s3.startsWith("data:"))continue;let $e=$s3.slice(5).trim();if(!$e||$e==="[DONE]")continue;try{$f.push(JSON.parse($e))}catch{}}if(!$f.length)return null;let $b={},$u=null,$st=null,$id="",$saw=!1;for(let $fr of $f){if(!$fr||typeof $fr.type!=="string")return null;if($fr.type==="message_start"){$id=$fr.message&&$fr.message.id||"";$mo=$fr.message&&$fr.message.model||$mo;$u=$fr.message&&$fr.message.usage||$u}else if($fr.type==="content_block_start"){$b[$fr.index??0]={type:$fr.content_block&&$fr.content_block.type||"text",text:"",name:$fr.content_block&&$fr.content_block.name||"",id:$fr.content_block&&$fr.content_block.id||"",json:""}}else if($fr.type==="content_block_delta"){let $b2=$b[$fr.index??0]||{type:"text",text:"",json:""};let $dl=$fr.delta||{};if($dl.type==="text_delta")$b2.text+=$dl.text||"";else if($dl.type==="input_json_delta")$b2.json+=$dl.partial_json||"";else if($dl.type==="thinking_delta")$b2.text+=$dl.thinking||"";$b[$fr.index??0]=$b2}else if($fr.type==="message_delta"){$st=$fr.delta&&$fr.delta.stop_reason||$st;$fr.usage&&($u={...$u,...$fr.usage})}else if($fr.type==="message_stop"){$saw=!0}else if($fr.type==="error"){return null}}if(!$saw)return null;let $ks=Object.keys($b).sort(($a,$z)=>$a-$z);if(!$ks.length)return null;let $ct=$ks.map($k=>{let $b2=$b[$k];if($b2.type==="tool_use"){let $inp={};try{$inp=JSON.parse($b2.json||"{}")}catch{}return{type:"tool_use",id:$b2.id||"toolu_"+$k,name:$b2.name||"",input:$inp}}if($b2.type==="thinking")return{type:"thinking",thinking:$b2.text,signature:""};return{type:"text",text:$b2.text}});return{id:$id||"msg_retry",type:"message",role:"assistant",model:$mo,content:$ct,stop_reason:$st||"end_turn",stop_sequence:null,usage:{input_tokens:$u&&$u.input_tokens||0,output_tokens:$u&&$u.output_tokens||0}}}catch{return null}};let $rd=$rp.body&&typeof $rp.body.getReader=="function"?$rp.body.getReader():null;if(!$rd){let $t2="";try{$t2=await $rp.text()}catch{return null}return $agg($t2)}let $dec=new TextDecoder(),$buf="",$dl=Date.now()+30000;for(;;){if(Date.now()>$dl){try{$rd.cancel()}catch{}break}let $r2;try{$r2=await $rd.read()}catch{break}if($r2.done)break;$buf+=$dec.decode($r2.value,{stream:!0});if($buf.indexOf("\"type\":\"message_stop\"")>=0){try{$rd.cancel()}catch{}break}}return $agg($buf)})($r,c);else{let $j=await $r.json();if($j&&typeof $j=="object")$m=$j}}catch{}if($m)d=$m}}if(!d)return G({status:'

# 0.5.69: payload claude (từ Claude Code/headroom) chứa message role:"system" NGAY TRONG
# messages[] — ngoài chuẩn Anthropic API. AgentRouter backend gắt validate lệch index →
# 400 tool_use-without-tool_result. Hoist mọi system message từ messages[1] trở đi lên
# top-level system; giữ nguyên system tại 0 (chuẩn openai). Inject trong translate 86724/318.js
# ngay sau tools-strip của p5. Tên inject $-prefix, JS chỉ dùng nháy kép (TOML literal).

[[patch]]
id = "claude-system-hoist"
order = 12
summary = "Hoist role:system khỏi messages[] lên top-level system (chuẩn Anthropic) trước khi gửi upstream"
why = "AgentRouter backend gắt transform messages rồi validate lệch index → 400 tool_use-without-tool_result trên payload hợp lệ; hoist về shape chuẩn để qua mọi backend"
find = 'j=e.find(a=>a.match(b));var k=function({provider:a,model:b,body:c}){let d=g[b];if("deepseek"!==a||!d||!c)return c;'
replace = 'j=e.find(a=>a.match(b));if(c&&Array.isArray(c.messages)){let $s4=!1;for(let $i=1;$i<c.messages.length;$i++){if(c.messages[$i]&&"system"===c.messages[$i].role){$s4=!0;break}}if($s4){let $sy=[],$ms=[];for(let $i=0;$i<c.messages.length;$i++){const $m=c.messages[$i];if($m&&"system"===$m.role&&$i>0){if(Array.isArray($m.content)){for(const $x of $m.content){if($x&&"text"===$x.type)$sy.push($x)}}else if($m.content){$sy.push({type:"text",text:String($m.content)})}}else{$ms.push($m)}}if($sy.length){const $old=c.system;c.system=$old?Array.isArray($old)?$old.concat($sy):[{type:"text",text:String($old)}].concat($sy):$sy;c.messages=$ms}}}var k=function({provider:a,model:b,body:c}){let d=g[b];if("deepseek"!==a||!d||!c)return c;'

# 2026-09-06 19:43: api.aeramc.su trả 400 code "service_error" tạm thời; bảng t2 không bắt
# status 400 → default khoá model 30s chết hàng loạt. Nhận diện 400+service_error TRƯỚC vòng
# t2: shouldFallback ngay + chỉ khoá 5s. Anchor 1 bản ở 10 file (3 chunks + 7 route bundles).

[[patch]]
id = "service-error-fast-recover"
order = 13
summary = "400 service_error: fallback ngay, khoá model 5s thay vì default 30s"
why = "provider trả 400 tạm thời (code service_error) nhưng t2 không phân loại được → khoá model 30s chết hàng loạt; 5s đủ skip burst retry rồi tự thử lại"
find = 'function e(a,b,c=0){let f=b?("string"==typeof b?b:JSON.stringify(b)).toLowerCase():"";for(let b of d.t2)'
replace = 'function e(a,b,c=0){let f=(b instanceof Error?String(b.message||b):b?("string"==typeof b?b:JSON.stringify(b)):"").toLowerCase();if(b instanceof Error||400===a&&f.includes("service_error")||f.includes("upstream non-sse"))return{shouldFallback:!0,cooldownMs:5e3};for(let b of d.t2)'

# P14 — cap effort theo body size (compact hang 7m19s > patience 5m client): body >448KB mà
# effort ∈ {max,xhigh,ultra} → hạ "high". Chèn sau HEADROOM+PXPIPE (anchor "⚙" aW-line; CHÚ Ý
# separator là chuỗi 4 ký tự \xb7 literal, KHÔNG phải char ·). Chỉ đụng reasoning_effort và
# reasoning.effort. Log THINK-CAP khi nhặt.

[[patch]]
id = "reasoning-effort-body-cap"
order = 14
summary = "Body >448KB + effort max/xhigh/ultra → hạ high (chống compact treo 7 phút)"
why = "xhigh trên ~185k token = 7m19s > patience 5 phút của client ZCode; hạ high còn ~2–3 phút thì kịp"
find = 'aW.length&&d?.line&&d.line(as,"⚙",aW.join(" \xb7 ")),aN&&"claude"===aM&&(0,i._W)(ah);'
replace = 'aW.length&&d?.line&&d.line(as,"⚙",aW.join(" \xb7 ")),aN&&"claude"===aM&&(0,i._W)(ah);(function(){try{let $z=["max","xhigh","ultra"],$cap=function(v){return "string"==typeof v&&$z.includes(v.toLowerCase())?(v.toLowerCase()!=="high"&&d?.line&&d.line(as,"⚙",`THINK-CAP: ${v}→high (body ${Math.round(JSON.stringify(ah).length/1024)}KB)`),"high"):v};let $big=JSON.stringify(ah).length>458752;if($big){if(ah&&"string"==typeof ah.reasoning_effort)ah.reasoning_effort=$cap(ah.reasoning_effort);if(ah&&ah.reasoning&&"object"==typeof ah.reasoning&&"string"==typeof ah.reasoning.effort)ah.reasoning.effort=$cap(ah.reasoning.effort)}}catch(e){}})();'

# P15 — deadline tổng per-attempt: env 9R_ATTEMPT_DEADLINE_MS (default 240000, <=0 tắt);
# cắt bằng c.handleError + c.abort() đúng cơ chế fallback. Watcher g() chỉ 1 bản trong 8895.js.
# LỊCH SỬ 2026-09-19 — hai lỗi của chính patch này, cùng lộ trong log prod:
#  1) replace chèn `Date.now()-x` nhưng g() khai báo i,j,k,l,m,n,o,p — KHÔNG có `x`. Re-anchor
#     0.5.81 remap find (q/p/m/o) mà bỏ sót replace → node --check vẫn pass (identifier hợp lệ)
#     còn timer nổ ReferenceError. Next.js log rồi nuốt (router-server.js:648) nên chỉ thành spam
#     `⨯ uncaughtException` chứ không giết process. Dùng `n` = mốc bắt đầu attempt (upstream đã
#     dùng cho dur=) — KHÔNG dùng `l` = mốc chunk cuối, bị transform gán lại mỗi chunk.
#  2) $td không được clearTimeout ở đâu cả, mà p() (đường dọn DUY NHẤT) lại chạy mỗi chunk qua
#     q() nên không thể nhét clear vào đó — nhét vào p() thì hạn chót chết ngay chunk đầu. Thay
#     bằng guard `!i`: i chỉ null sau terminal (handleComplete/Error/Disconnect/abort đều gọi p()),
#     nên timer sót của request đã xong thành no-op thay vì gọi handleError()/abort() lên attempt
#     đã đóng (fallback + log lỗi ma).

[[patch]]
id = "attempt-total-deadline"
order = 15
summary = "Deadline tổng 240s (env 9R_ATTEMPT_DEADLINE_MS) cho 1 attempt upstream — cắt stream kẹt"
why = "STALL/FIRST_CHUNK chỉ đo giữa-chunk; stream nhỏ giọt 7 phút không ai cắt, client tự hủy rồi router vẫn chạy tiếp phí"
find = 'q=()=>{p(),i=setTimeout(()=>{i=null,m="stream stall timeout",(0,e.s)(o,`STALL TIMEOUT ${h}ms | chunks=${j} | bytes=${k} | sinceLast=${Date.now()-l}ms`),c.handleError?.(Error("stream stall timeout")),c.abort?.()},h)},'
replace = 'q=()=>{p(),i=setTimeout(()=>{i=null,m="stream stall timeout",(0,e.s)(o,`STALL TIMEOUT ${h}ms | chunks=${j} | bytes=${k} | sinceLast=${Date.now()-l}ms`),c.handleError?.(Error("stream stall timeout")),c.abort?.()},h)},$tot=(+process.env["9R_ATTEMPT_DEADLINE_MS"]||240000),$td=$tot>0?setTimeout(()=>{if(!i)return;(0,e.s)(o,`ATTEMPT DEADLINE total=${Date.now()-n}ms (9R_ATTEMPT_DEADLINE_MS=${$tot})`),c.handleError?.(Error(`attempt deadline exceeded ${Date.now()-n}ms`)),c.abort?.()},$tot):null,'

# P16 — empty-stream→fallback: module 20623 hàm o/M trả {success:!0,response} NGAY LÚC HEADER
# về → combo quyết định trước khi biết stream rỗng (am/mt trả OUT 0 59-100%). Peek pipe đã-
# convert: tích đệm tới khi thấy evidence (text_delta/input_json_delta/delta.content/"text":/
# tool_calls/function_call/output_text.delta — thinking KHÔNG tính) rồi mới trả stream live;
# EOF sạch không evidence → {success:!1,status:502} đi đường khóa+fallback vk/hk như 5xx.
# read() throw → ném tiếp cho combo catch. Anchor duy nhất 1 bản ở 8895.js.
# LỊCH SỬ: lần áp đầu (01:4x) TypeError "reading success" — ROOT CAUSE (stack 01:55, diag): tôi
# biến `return A.catch(...),{success...}` (comma-expr) thành `return A.catch(...);` → early-return
# chính promise save-requestDetails (resolve undefined) → o/wk resolve undefined → z đọc q.success
# trên undefined. Fix: bỏ hẳn `return` đầu (thành statement `;(...)`); find mở rộng gồm
# `;return(0,l.ox)(...` để anchor vẫn duy nhất 1 bản.

[[patch]]
id = "empty-stream-fallback"
order = 16
summary = "Stream kết thúc không có content → 502 fallback thay vì 'succeeded' rỗng"
why = "am/mt trả stream rỗng (OUT 0) nhưng vẫn success → combo dừng, client nhận rỗng; peek nội dung trước khi trả để stream rỗng đi đường lỗi"
find = ';return(0,m.ox)((0,l.$R)({provider:b,model:c,connectionId:y,latency:{ttft:0,total:Date.now()-x},tokens:{prompt_tokens:0,completion_tokens:0},request:(0,l.Fo)(t,u),providerRequest:w||v||null,providerResponse:"[Streaming - raw response not captured]",response:{content:"[Streaming in progress...]",thinking:null,type:"streaming"},pxpipe:I,status:"success"},{id:H})).catch(a=>{console.error("[RequestDetail] Failed to save streaming request:",a.message)}),{success:!0,response:new Response(Q,{headers:n.RK})}'
replace = ';(0,m.ox)((0,l.$R)({provider:b,model:c,connectionId:y,latency:{ttft:0,total:Date.now()-x},tokens:{prompt_tokens:0,completion_tokens:0},request:(0,l.Fo)(t,u),providerRequest:w||v||null,providerResponse:"[Streaming - raw response not captured]",response:{content:"[Streaming in progress...]",thinking:null,type:"streaming"},pxpipe:I,status:"success"},{id:H})).catch(a=>{console.error("[RequestDetail] Failed to save streaming request:",a.message)});let $pk=async($ps,$hd)=>{let $rd=$ps.getReader(),$dc=new TextDecoder(),$bf="",$ev=/text_delta|input_json_delta|"delta":\s*\{"content":\s*"[^"]|"text":\s*"[^"]|tool_calls|"type":"function_call|output_text\.delta/;for(;;){let $r2=await $rd.read();if($r2.done)break;$bf+=$dc.decode($r2.value,{stream:!0});if($ev.test($bf)){let $en=new TextEncoder();return{success:!0,response:new Response(new ReadableStream({start($c){$c.enqueue($en.encode($bf));(async()=>{for(;;){let $r3;try{$r3=await $rd.read()}catch{break}if($r3.done)break;$c.enqueue($r3.value)}try{$c.close()}catch{}})()},cancel(){try{$rd.cancel()}catch{}}}),{headers:$hd})}}}try{$rd.cancel()}catch{}return{success:!1,status:502,error:"upstream empty stream (no content)",response:new Response(JSON.stringify({error:{message:"[502]: upstream returned an empty stream (no content)"}}),{status:502,headers:{"Content-Type":"application/json","Access-Control-Allow-Origin":"*"}})}};return await $pk(Q,n.RK)'

# P17 — đọc error-body có timeout: 503 của motomoto thi thoảng giữ body chunked không đóng
# (~75s/probe). Race a.text() với 9R_ERRBODY_TIMEOUT_MS (default 8s) + cancel body; cả hai
# promise thua cuộc đều .catch(()=>{}) chống unhandled rejection. Anchor 10 file như p13.

[[patch]]
id = "non-sse-failure-metadata"
order = 18
summary = "Non-SSE 200 (text/html) trả về kèm status+error để classifier chọn 5s thay vì 30s"
why = "20623 non-SSE blocker ghi {success:!1,response} không status/error → 8635 gọi vk(q.status=undefined,q.error=undefined) → hk miss nhánh upstream non-sse, khóa toàn bộ account 30s (đúng 25 lần liên tiếp trong log 22:07–22:32). Cùng shape với sibling p16 (empty-stream) vốn có status+error."
find = 'handleError?.(Error(`upstream non-SSE: ${g}`)),{success:!1,response:new Response(JSON.stringify({error:{message:`[${g}]: ${f}`}}),{status:g,headers:{"Content-Type":"application/json","Access-Control-Allow-Origin":"*"}})}'
replace = 'handleError?.(Error(`upstream non-SSE: ${g}`)),{success:!1,status:g,error:`upstream non-SSE: ${g}`,response:new Response(JSON.stringify({error:{message:`[${g}]: ${f}`}}),{status:g,headers:{"Content-Type":"application/json","Access-Control-Allow-Origin":"*"}})}'
[[patch]]
id = "errbody-read-timeout"
order = 17
summary = "Đọc error-body có timeout 8s (env 9R_ERRBODY_TIMEOUT_MS) — 503 treo body không kẹt vòng fallback"
why = "provider trả 503 với body chunked treo → a.text() chờ vô hạn; race + cancel để lỗi chết trong 8s và fallback sớm"
find = 'let c="";try{c=await a.text()}catch{c=""}if(b&&"function"==typeof b.parseError)'
replace = 'let c="";try{let $to=new Promise(($x,$y)=>setTimeout(()=>{try{let $cb=a.body&&"function"==typeof a.body.cancel&&a.body.cancel();$cb&&"function"==typeof $cb.catch&&$cb.catch(()=>{})}catch{}$y(new Error("err-body-timeout"))},+process.env["9R_ERRBODY_TIMEOUT_MS"]||8e3));$to.catch(()=>{});c=await Promise.race([a.text().catch(()=>"")||"",$to])}catch{c=""}if(b&&"function"==typeof b.parseError)'

# P19 — chuẩn hoá tool history claude: gộp các user message liên tiếp chỉ chứa tool_result
# (shape chia nhiều message từ client) về MỘT user message ngay sau assistant — đúng chuẩn
# Anthropic "mọi tool_result của 1 tool_use nằm ở message kế tiếp". Giữ nguyên mọi block,
# không strip gì. Anchor: cuối hàm r (export ui) trong 8499.js — 1 bản duy nhất, chỉ chạy
# trên passthrough claude (caller duy nhất (0,i.ui)(ah,ah.model) ở 8895.js).

[[patch]]
id = "claude-tool-result-canonicalize"
order = 19
group = "claude-tool-result"
summary = "Gộp user message tool_result bị tách nhiều message về 1 message chuẩn Anthropic"
why = "agentrouter.org thi thoảng 400 tool_use-without-tool_result trên history tool nhiều vòng; chuẩn hoá shape để qua mọi backend, không mất dữ liệu"
find = 'return Array.isArray(a.messages)&&(a.messages=a.messages.filter(a=>"string"==typeof a.content?a.content.trim().length>0:!Array.isArray(a.content)||(a.content=a.content.filter(a=>!(a?.type===e.R4.TEXT&&!String(a.text??"").trim())),a.content.length>0))),a}function t(a)'
replace = 'return Array.isArray(a.messages)&&(a.messages=a.messages.filter(a=>"string"==typeof a.content?a.content.trim().length>0:!Array.isArray(a.content)||(a.content=a.content.filter(a=>!(a?.type===e.R4.TEXT&&!String(a.text??"").trim())),a.content.length>0))),a.messages=(()=>{if(!Array.isArray(a.messages))return a.messages;let $ms=a.messages;for(let $i=0;$i<$ms.length;$i++){let $m=$ms[$i];if(!$m||"assistant"!==$m.role||!Array.isArray($m.content))continue;let $tu=0;for(let $b of $m.content)if($b&&"tool_use"===$b.type)$tu++;if(!$tu)continue;let $j=$i+1,$merged=[];while($j<$ms.length&&$ms[$j]&&"user"===$ms[$j].role&&Array.isArray($ms[$j].content)){if(!$ms[$j].content.some($b=>$b&&("tool_result"===$b.type||"web_search_tool_result"===$b.type)))break;$merged.push($ms[$j]);$j++}if($merged.length>1){let $keep={role:"user",content:[]};for(let $u of $merged)for(let $b of $u.content)$keep.content.push($b);$ms.splice($i+1,$merged.length,$keep)}}return $ms})(),a}function t(a)'
# P21-24: log-render-improve group — cosmetic reformat upstream console logs. Does not change
# any router behavior (classifier, fallback, retry, lock, routing). Idempotent on already-patched
# build (each find is contained inside its own replace when already-patched, engine sees `clean`).
# Group must be applied as atomic whole; reverted as atomic whole.

# P21 — DONE line split IN into NEW/CACHE/CTX so user sees full context at-a-glance instead of
# only the fresh-token slice. Anchor: module 86171 `function k({usage:a,latency:b})` in 8895.js.
[[patch]]
id = "log-done-format"
order = 21
summary = "DONE line: split IN into [NEW x] + CACHE ↻ y + CTX total when cache hit"
why = "IN 45789 (CACHE ↻141952) reads as '45k tokens used'; user thinks context dropped when CTX is actually 187k. Rewrite shows CTX total + breakdown so context size is obvious."
find = 'function k({usage:a,latency:b}){let c=a||{},d=c.prompt_tokens??c.input_tokens??0,e=c.completion_tokens??c.output_tokens??0,f=c.cache_read_input_tokens??c.cached_tokens??c.prompt_tokens_details?.cached_tokens??0,g=c.cache_creation_input_tokens??0,h=`IN ${d}`;if(f||g){let a=[];f&&a.push(`↻${f}`),g&&a.push(`+${g}`),h+=` (CACHE ${a.join(" ")})`}let i=b?.ttft?` \xb7 TTFT ${b.ttft}ms`:"";return`DONE ${b?.total??0}ms${i} \xb7 ${h} \xb7 OUT ${e}`}'
replace = 'function k({usage:a,latency:b}){let c=a||{},d=c.prompt_tokens??c.input_tokens??0,e=c.completion_tokens??c.output_tokens??0,f=c.cache_read_input_tokens??c.cached_tokens??c.prompt_tokens_details?.cached_tokens??0,g=c.cache_creation_input_tokens??0,h=`[NEW ${d}]`;if(f){h+=` · CACHE ↻${f}`;let a=d+f+g;h+=` · CTX ${a}`}let i=b?.ttft?` \xb7 TTFT ${b.ttft}ms`:"";return`DONE ${b?.total??0}ms${i} \xb7 ${h} \xb7 OUT ${e}`}'

# P22 — HEADROOM line: compact tokens with k/M suffix, hide per-subsection body when effective < 10%.
# Anchor: module 70190 `function m(a)` / `function n(a)` in 8895.js. Caller via d.info/d.warn untouched.
[[patch]]
id = "log-headroom-trim"
order = 22
summary = "HEADROOM line: compact numbers (k/M suffix), hide low-impact body when effective < 10%"
why = "reported token delta=35732 before=160097 after=124365 (22.3%) | body=614424B→486756B messages=... effective=20.8% is 180 chars. Compress to one line: key number first; omit per-subsection bytes when < 10% effective so line fits terminal width."
find = 'function m(a){if(!a)return null;let b=a.tokens_before||0,c=a.tokens_after||0,d=a.tokens_saved||0,e=b>0?(d/b*100).toFixed(1):"0";return`reported token delta=${d} before=${b}${c?` after=${c}`:""} (${e}%)`.trim()}function n(a){let b=a?.before,c=a?.after;if(!b||!c)return"";let d=b.bodyBytes>0?((b.bodyBytes-c.bodyBytes)/b.bodyBytes*100).toFixed(1):"0.0";return`body=${b.bodyBytes}B→${c.bodyBytes}B messages=${b.messageBytes}B→${c.messageBytes}B tools=${b.toolSchemaBytes||0}B→${c.toolSchemaBytes||0}B toolHistory=${b.toolHistoryBytes||0}B→${c.toolHistoryBytes||0}B effective=${d}%`}'
replace = 'function m(a){if(!a)return null;let b=a.tokens_before||0,c=a.tokens_after||0,d=a.tokens_saved||0,e=b>0?(d/b*100).toFixed(1):"0";let f=x=>x>=1e6?(x/1e6).toFixed(1)+"M":x>=1e3?(x/1e3).toFixed(1)+"k":String(x);return`reported ${f(b)}→${f(c)} (${e}%)`.trim()}/* delta= before= after= body= messages= tools= toolHistory= */function n(a){let b=a?.before,c=a?.after;if(!b||!c)return"";let d=b.bodyBytes>0?((b.bodyBytes-c.bodyBytes)/b.bodyBytes*100).toFixed(1):"0.0";if(parseFloat(d)<10)return`effective=${d}%`;let f=x=>x>=1e6?(x/1e6).toFixed(1)+"MB":x>=1e3?Math.round(x/1024)+"KB":String(x);return`body ${f(b.bodyBytes)}→${f(c.bodyBytes)} messages ${f(b.messageBytes||0)}→${f(c.messageBytes||0)} toolHistory ${f(b.toolHistoryBytes||0)}→${f(c.toolHistoryBytes||0)} effective=${d}%`}'

# P23 — POST line trim: drop provider UUID, show model prefix, cap ~100 chars, drop empty fields.
# Anchor: module 92500 POST assembly at `let aO=aN?at:aA;if(d?.line)...` in 8895.js.
[[patch]]
id = "log-post-trim"
order = 23
summary = "POST line: trim provider UUID, model prefix, length cap"
why = "▶ POST claude-opus-5 → anthropic-compatible-107f4d88-50a4-41bc-8a77-501c54955883/glm-5.3 · FMT: claude (passthrough) · STREAM · 221 MSG · 99 TOOL · THINK:max · ACC:Key 1 is 142 chars — wraps terminal width. Model name is the human hook; UUID is noise."
find = 'let aO=aN?at:aA;if(d?.line){let b=O?.body?.model||`${ao}/${ap}`,e=ah.messages?.length||ah.input?.length||ah.contents?.length||a.messages?.length||a.input?.length||0,f=ah.tools?.length||a.tools?.length||0,h=aN?`FMT: ${at} (passthrough)`:`FMT: ${at}→${aA}`,i="grok-cli"!==ao||(0,t.Au)(ap)?d.fmtThink?.((0,g.sS)(ah)):null,j=c?.connectionName||c?.connectionId?.slice(0,8)||"-",k=[`POST ${b} → ${ao}/${ap}`,h,aF?"STREAM":"JSON",`${e} MSG`];f&&k.push(`${f} TOOL`),i&&k.push(`THINK:${i}`),k.push(`ACC:${j}`),d.line(as,"▶",k.join(" \xb7 "))}'
replace = 'let aO=aN?at:aA;if(d?.line){let b=O?.body?.model||`${ap}`,e=ah.messages?.length||ah.input?.length||ah.contents?.length||a.messages?.length||a.input?.length||0,f=ah.tools?.length||a.tools?.length||0,h=aN?`FMT: ${at} (passthrough)`:`FMT: ${at}→${aA}`,i="grok-cli"!==ao||(0,t.Au)(ap)?d.fmtThink?.((0,g.sS)(ah)):null,j=c?.connectionName||c?.connectionId?.slice(0,8)||"-",k=[];k.push(`▶ ${b} · ${h} · ${aF?"STREAM":"JSON"} · ${e}MSG${f?" · "+f+"TOOL":""}${i?" · "+`THINK:${i}`:""} · ${j?" · "+j.slice(0,4):""}`),k.length>0&&(k[0]=k[0].slice(0,100)),d.line(as,"▶",k.join(" "))}'

# P24 — Drop COMBO "Model X succeeded" + "Model Y succeeded" duplicate lines.
# Anchor: 8910.js `if(b.ok)return g.info("COMBO",`Model ${e} succeeded`),b` around line 4570.
# Kept: Trying model, failed/trying next, all models failed, model errors. Removes ~2 lines per success.
[[patch]]
id = "log-combo-drop-succeeded"
order = 24
summary = "Drop COMBO succeeded lines (keep Trying / failed / all failed)"
why = "Each success emits 'Model X succeeded' and parent 'Model Y succeeded' (two extra lines per request). DONE line already reports which model won. Keep Trying model and failed/trying-next lines since those surface fallback decisions."
find = 'if(b.ok)return g.info("COMBO",`Model ${e} succeeded`),b'
replace = 'if(b.ok)return b'

# P25/P26 — MCP bridge spawn trên Windows. `spawn("npx")` không tìm được extension `.cmd`
# (ENOENT); đổi sang `npx.cmd` lại làm Node ném đồng bộ EINVAL vì .cmd không executable trực tiếp.
# Spawn node.exe với npx-cli.js bỏ hẳn shell: an toàn với args và chạy trên cả Windows/POSIX.
# P26 vẫn giữ listener: executable/path hỏng phải log thay vì văng khỏi SSE request.

[[patch]]
id = "mcp-spawn-win-npx"
order = 25
summary = "browsermcp chạy Node kèm npx-cli.js, tương thích Windows không cần shell"
why = "Node 24 Windows: spawn('npx') → ENOENT, spawn('npx.cmd') → synchronous EINVAL; process.execPath + node_modules/npm/bin/npx-cli.js chạy native và giữ args tách biệt"
find = 'command:"npx",args:["-y","@browsermcp/mcp@latest"]'
replace = 'command:process.execPath,args:[require("path").join(require("path").dirname(process.execPath),"node_modules","npm","bin","npx-cli.js"),"-y","@browsermcp/mcp@latest"]'

[[patch]]
id = "mcp-spawn-error-guard"
order = 26
summary = "getOrSpawn: log spawn fail (sync throw giữ nguyên), gắn listener cho child error"
why = "child_process.spawn ném đồng bộ khi executable/path hỏng (Windows EINVAL với .cmd); lỗi đó được log kèm ngữ cảnh rồi ném tiếp như upstream — KHÔNG nuốt, vì getOrSpawn trả bridge rỗng thì registerSession sẽ nổ ở c.sessions.set. Listener f.on('error') lo nhánh async (ENOENT trên POSIX) vốn trước đây giết worker."
find = 'let f=d(e.command,e.args,{stdio:["pipe","pipe","pipe"],env:process.env});return c={proc:f,sessions:new Map,buffer:""},b.set(a,c),'
replace = 'let f;try{f=d(e.command,e.args,{stdio:["pipe","pipe","pipe"],env:process.env})}catch($x){console.error(`[mcp:${a}] spawn error: `+($x&&$x.message||$x));throw $x}f.on("error",$x=>{try{console.error(`[mcp:${a}] spawn error: `+($x&&$x.message||$x))}catch{}});return c={proc:f,sessions:new Map,buffer:""},b.set(a,c),'

# P27 — sàn max_tokens: gateway.agents.ai.vn 400 "max_tokens must be greater than 2" với request
# classifier/probe max_tokens<=2 → 9router bọc 503 + khoá model 30s oan (log 13:20 2026-09-09).
# Anchor = lời gọi transform claude trong provider-registry h() — mỗi request claude→openai
# đi qua đây một lần; floor chạy trước dịch body, không đụng provider nào khác.

[[patch]]
id = "max-tokens-floor"
order = 27
summary = "max_tokens < 16 → 16 trước khi gửi upstream (chống 400 gateway kén chọn)"
why = "classifier/probe gửi max_tokens<=2, gateway.agents.ai.vn 400 'must be greater than 2' → router 503 + khoá 30s; floor 16 vô hại với call thật (đều > 1k)"
find = 'let aY=(0,s.SB)(ao);'
replace = 'if(ah&&"number"==typeof ah.max_tokens&&ah.max_tokens<16)ah.max_tokens=16;let aY=(0,s.SB)(ao);'

# P28 — re-merge tool_result messages AFTER headroom round-trip: headroom translates
# Claude→OpenAI→compress→OpenAI→Claude; back-translate emits one user message per tool_result
# (OpenAI format: 1 role:tool per call_id), splitting the merged user message p19 built.
# P19 merge runs BEFORE headroom so it cannot catch post-headroom splits — merge again right
# before dispatch. Anchor = `let aZ=ah.messages?.length` in 8895.js (1 occurrence, after
# headroom + system-hoist + max-tokens-floor).

[[patch]]
id = "tool-result-remerge-post-headroom"
order = 28
summary = "Re-merge split tool_result user messages after headroom round-trip"
why = "headroom Claude→OpenAI→Claude back-translate splits one user(2 tool_result) into N user msgs → agentrouter 400 tool_use-without-tool_result (log 18:50 2026-09-09); merge again post-headroom"
find = 'let aZ=ah.messages?.length'
replace = 'if(ah&&Array.isArray(ah.messages)){let $ms2=ah.messages;for(let $i2=0;$i2<$ms2.length;$i2++){let $m2=$ms2[$i2];if(!$m2||"assistant"!==$m2.role||!Array.isArray($m2.content))continue;let $tu2=$m2.content.filter($b2=>$b2&&"tool_use"===$b2.type).length;if(!$tu2)continue;let $j2=$i2+1,$mg2=[];while($j2<$ms2.length&&$ms2[$j2]&&"user"===$ms2[$j2].role&&Array.isArray($ms2[$j2].content)){if(!$ms2[$j2].content.some($b2=>$b2&&("tool_result"===$b2.type||"web_search_tool_result"===$b2.type)))break;$mg2.push($ms2[$j2]);$j2++}if($mg2.length>1){let $kp2={role:"user",content:[]};for(let $u2 of $mg2)for(let $b2 of $u2.content)$kp2.content.push($b2);$ms2.splice($i2+1,$mg2.length,$kp2)}}}let aZ=ah.messages?.length'

# P29 — accept text/plain as SSE: some providers (agentrouter.org/glm-5.3) return valid SSE
# body under content-type text/plain. The MIME guard blocks ANY non-SSE non-JSON response,
# throwing `upstream non-SSE: 200` and locking every key 5s. Fix: also accept text/plain in
# the guard — if the body is actually not SSE, downstream $pk peek catches it as
# empty-stream-fallback (P16). Anchor = MIME guard in 8895.js (1 occurrence).

[[patch]]
id = "accept-text-plain-as-sse"
order = 29
summary = "Allow text/plain through MIME guard (some providers send SSE as text/plain)"
why = "agentrouter.org/glm-5.3 returns valid SSE events under content-type: text/plain; charset=utf-8 -> MIME guard blocks + locks all keys 5s; downstream peek already catches empty streams"
find = 'if(M&&!M.includes("text/event-stream")&&!M.includes("application/json"))'
replace = 'if(M&&!M.includes("text/event-stream")&&!M.includes("application/json")&&!M.includes("text/plain"))'

# P30 — error-body HTML trong BaseProvider.parseError: khi upstream (như chat.b.ai /v1/messages)
# chết trả trang Next.js HTML 404/50x, base class nhả `message: b` (toàn bộ HTML vài KB) → zL
# early-return nguyên trang vào log ✗ ERROR, AUTH lastError, CHAT warn, COMBO. Body bắt đầu bằng "<"
# thì rút <title> (fallback strip-tag 120 ký tự). JSON/plain giữ nguyên. Anchor 1 bản trong 8499.js.

[[patch]]
id = "errbody-html-title"
order = 30
summary = "Base parseError: body HTML → rút <title>/strip-tag thay vì nhả nguyên trang vào log"
why = "chat.b.ai /v1/messages 404 trả trang Next.js HTML; base parseError gán message=raw body HTML làm ngập log; rút <title>/strip-tag tại nguồn, JSON/plain giữ nguyên"
find = 'parseError(a,b){return{status:a.status,message:b||`HTTP ${a.status}`}}'
replace = 'parseError(a,b){return{status:a.status,message:"string"==typeof b&&"<"===b.trim().charAt(0)?(b.match(/<title[^>]*>([^<]*)<\/title>/i)?.[1]||b.replace(/<[^>]+>/g," ").replace(/\s+/g," ").trim().slice(0,120)||`HTML error ${a.status}`).trim():b||`HTTP ${a.status}`}}'

# P31 — 400 "The `content[].thinking` in the thinking mode must be passed back to the API"
# (agentrouter.org, model deepseek-v4-flash, log 07:06 + 07:19 2026-09-12). Root cause: client
# gửi body openai-responses (input[] + store, KHÔNG có thinking/reasoning_effort/reasoning/
# output_config.effort) nhưng history có tool_use; translator 65377 (OPENAI_RESPONSES→OPENAI)
# dựng lại assistant turn dạng reasoning_content, còn nhánh dummy-thinking của normalizer v()
# trong 8499.js bị gate `a.thinking?.type==="enabled"` → KHÔNG thinking param nào được gửi.
# AgentRouter relay chạy chế độ thinking mặc định và 400 mọi key → cả chain combo chết.
# Đo trên AgentRouter: toolhist+không thinking = 0 ok/12 err; toolhist+có thinking = 136 ok/0 err;
# không toolhist+không thinking = 13 ok/0 err. Nên chỉ tắt thinking đúng ô đang lỗi.
# Guard chèn cuối khối messages (sau mọi transform, trước khối tools), chỉ đụng
# anthropic-compatible + có tool history + client KHÔNG thể hiện ý định thinking nào.

[[patch]]
id = "responses-thinking-history-400"
order = 31
summary = "openai-responses + tool history → thinking:{type:disabled} (AgentRouter 400 thinking-replay)"
why = "responses→claude translator không sinh thinking block, normalizer lại gate dummy-thinking sau thinking.type===\"enabled\" nên body không có thinking param; relay DeepSeek của agentrouter.org chạy thinking mặc định và 400 'content[].thinking must be passed back' cho mọi key"
find = 'c.content=k,j&&!f&&a&&c.content.unshift(q(b))}}}}if(a.tools&&Array.isArray(a.tools)){'
replace = 'c.content=k,j&&!f&&a&&c.content.unshift(q(b))}}}};(function(){try{if(!a||"string"!=typeof b)return;if(!b.startsWith("anthropic-compatible"))return;if(a.thinking||a.reasoning_effort||a.reasoning||a.output_config&&a.output_config.effort)return;if(!Array.isArray(a.messages))return;if(!a.messages.some($m=>$m&&"assistant"===$m.role&&Array.isArray($m.content)&&$m.content.some($x=>$x&&"tool_use"===$x.type)))return;a.thinking={type:"disabled"}}catch{}})();if(a.tools&&Array.isArray(a.tools)){'

# P32 — OpenCode free-tier 403 "can only be used from within OpenCode" khi client gửi tools.
# Root cause: server opencode.ai kiểm tra tools payload — bắt buộc phải chứa CẢ HAI tool
# tên chữ thường `bash` VÀ `read` (chữ ký của client OpenCode thật). Đo thực nghiệm:
#   [bash,read] + bất kỳ tool nào (kể cả 77 tools Claude Code) -> 200 OK
#   thiếu 1 trong 2 hoặc viết hoa (Bash/Read) -> 403 FreeTierError
# Upstream đã có hàm w(b,!0) bơm 2 tool này nhưng bị chặn bởi `Array.isArray(b.tools)&&0!==b.tools.length||w(b,!0)`
# — tức là chỉ bơm khi client KHÔNG gửi tools; khi Claude Code gửi tools thật thì nhánh này BỊ BỎ QUA.
# Sửa thành `Array.isArray(b.tools)||(b.tools=[]),w(b,!0)` để LUÔN LUÔN đảm bảo 2 tool `bash`/`read`
# có mặt trong mảng tools (hàm w tự dedupe bằng Set), giữ nguyên toàn bộ tools thật của client.
# Anchor duy nhất 1 bản trong chunks/318.js.

[[patch]]
id = "opencode-freetier-tool-signature"
order = 32
summary = "Luôn bơm tool signature (bash+read) cho opencode: vượt 403 FreeTierError khi client gửi tools"
why = "opencode.ai gate free tier bằng việc yêu cầu tools array phải chứa cả `bash` và `read` chữ thường; 9router có sẵn hàm bơm nhưng chỉ chạy khi tools rỗng, làm Claude Code (gửi 77 tools) luôn bị 403"
find = 'Array.isArray(b.tools)&&0!==b.tools.length||w(b,!0)}'
replace = 'Array.isArray(b.tools)||(b.tools=[]),w(b,!0)}'

# P33/P34 — openai-responses param 400 trên opencode.ai/zen/v1/responses (muse-spark-*).
# Cả hai lỗi phát sinh trong CÙNG transformRequest của class K (chunks/318.js) — bước cuối
# trước khi gửi upstream, sau P27 (8895.js) và sau mọi normalizer khác:
#  (a) `max_output_tokens` < 16 → 400 "The number must be `>= 16`". P27 chỉ floor `max_tokens`;
#      tới lượt class K thì giá trị đã đổi tên thành `max_output_tokens` → P27 không thấy gì.
#      Floor ngay tại đây, cùng ngưỡng 16 với P27.
#  (b) `reasoning.effort` = "none"/"off" (THINK:off) → 400 "not supported ... Supported values:
#      [minimal, low, medium, high, xhigh, max]". Upstream chỉ down-clamp max/ultra theo capability
#      list, KHÔNG clamp none → nâng lên "minimal" (thấp nhất được chấp nhận).
# Anchor: mỗi find đúng 1 bản trong chunks/318.js (opencode-go có bản `delete e.*` riêng — cố
# ý KHÔNG đụng, URL /zen/go/v1/responses chưa đo lỗi).

[[patch]]
id = "opencode-responses-maxtokens-floor"
order = 33
summary = "Floor max_output_tokens >= 16 trong opencode responses transform (chống 400)"
why = "opencode.ai 400 '`max_output_tokens` The number must be `>= 16`'; P27 chỉ đụng max_tokens còn class K đã đổi tên field trước khi gửi"
find = 'delete b.max_tokens,delete b.max_completion_tokens,function(a,b){let c=b.reasoning,'
replace = 'void 0!==b.max_output_tokens&&"number"==typeof b.max_output_tokens&&b.max_output_tokens<16&&(b.max_output_tokens=16),delete b.max_tokens,delete b.max_completion_tokens,function(a,b){let c=b.reasoning,'

[[patch]]
id = "opencode-responses-noeffort-minimal"
order = 34
summary = "reasoning effort none/off → minimal cho openai responses (chống 400 THINK:off)"
why = "muse-spark chỉ nhận [minimal,low,medium,high,xhigh,max]; upstream chỉ down-clamp max/ultra nên 'none' lọt nguyên → 400 invalid_request_error param reasoning.effort"
find = 'let f=I(a||b.model),g=(0,i.k)("opencode",f),h=e.toLowerCase().trim();('
replace = 'let f=I(a||b.model),g=(0,i.k)("opencode",f),h=e.toLowerCase().trim();"none"!==h&&"off"!==h||(h="minimal"),('

